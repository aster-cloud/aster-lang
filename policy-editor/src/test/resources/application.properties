# encoding: UTF-8
# This file is encoded in UTF-8 to support Chinese comments
# Test-specific configuration
# Use random available port for tests to avoid conflicts with dev server
%test.quarkus.http.test-port=0
# 禁用 Keycloak DevServices，避免在 CI/PODMAN 环境中拉起容器
%test.quarkus.keycloak.devservices.enabled=false
%test.quarkus.oidc.devservices.enabled=false
%test.quarkus.oidc.enabled=false
