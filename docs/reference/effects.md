# Effects

Aster functions can declare effects performed by their body. Currently supported effects are:

- `IO`
- `CPU`

Example:

```text
To login with user: Text and pass: Text, produce Result of User or AuthErr. It performs IO:
  ...
```

