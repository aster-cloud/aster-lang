# Types

Aster types include:

- `Text`, `Int`, `Float`, `Bool`
- `maybe T`
- `Option of T` (distinct from `maybe T`)
- `Result of Ok or Err`
- `list of T`, `map K to V`

Type identifiers are capitalized.

