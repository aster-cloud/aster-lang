# Core IR

A small, strict intermediate representation with effect enum.

```ts
import { Core, Effect } from 'aster-lang';
```

- `Core.Module`, `Core.Func`, `Core.Data`, `Core.Enum`
- `Effect.IO`, `Effect.CPU`

