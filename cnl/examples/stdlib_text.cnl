This module is std.text.
To concat with left: Text, right: Text, produce Text:
  Return Text.concat(left, right).

To hasSubstring with haystack: Text, needle: Text, produce Bool:
  Return Text.contains(haystack, needle).
