This module is test.eff.violation.files_calls_secrets.
To get_api_key, produce Text. It performs io [Secrets]:
  Return Secrets.get("API_KEY").

To write_log, produce Text. It performs io [Files]:
  Let key be get_api_key().
  Return File.write("/tmp/log.txt", key).
