This module is demo.lambdamatchresult.
To fromResult with r: Result of Text and Text, produce Text:
  Let f be function with x: Result of Text and Text, produce Text:
    Match x:
      When Ok(v), Return v.
      When Err(e), Return Text.concat("E:", e).
  Return f(r).
