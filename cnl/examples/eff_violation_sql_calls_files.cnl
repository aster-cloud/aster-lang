This module is test.eff.violation.sql_calls_files.
To read_config, produce Text. It performs io [Files]:
  Return File.read("/etc/config.ini").

To db_init, produce Text. It performs io [Sql]:
  Let config be read_config().
  Return Db.connect(config).
