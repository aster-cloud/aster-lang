This module is demo.map.

To val with m: map Text to Text and k: Text, produce Text?:
  Return Map.get(m, k).

To valStrict with m: map Text to Text and k: Text, produce Text:
  Return Map.get(m, k).
