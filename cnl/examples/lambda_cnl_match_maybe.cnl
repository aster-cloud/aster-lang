This module is demo.lambdamatchmaybe.

To fromMaybe with x: maybe Text and d: Text, produce Text:
  Let f be function with x: maybe Text, produce Text:
    Match x:
      When null, Return d.
      When v, Return v.
  Return f(x).

