This module is demo.lambdamixed.

To demo with pfx: Text, produce Text:
  Let f be function with x: Text, produce Text:
    Return Text.concat(pfx, x).
  Let g be (y: Text) => Text.concat("2", y).
  Return Text.concat(f("a"), g("b")).

To compare with x: Int and y: Int, produce Bool:
  Let cmp be (x1: Int, y1: Int) => x1 < y1.
  Return cmp(x, y).
