This module is demo.rest.
To greet with name: Text, produce Text:
  Return Text.concat("Hello, ", name).

To exclaim with s: Text, produce Text:
  Return Text.concat(s, "!").

To length with s: Text, produce Int:
  Return Text.length(s).
