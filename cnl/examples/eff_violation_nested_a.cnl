This module is test.eff.violation.nested_a.
To inner_io, produce Text. It performs io [Http]:
  Return Http.get("/ping").

To middle_cpu, produce Text. It performs cpu:
  Return inner_io().

To outer_pure, produce Text:
  Return middle_cpu().
