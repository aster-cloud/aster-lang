apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: aster-policy
  labels:
    app: redis
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: aster-platform
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app: redis
