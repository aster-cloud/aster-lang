This module is foo.bar.

# Fan-out concurrency workflow example.
# After init_context completes, it fans out to other steps.

To sampleWorkflow, produce Result of Text with IO. It performs io:

  workflow:
    step one:
      return ok of "x".

      compensate:
        return ok of "undo".

  .
