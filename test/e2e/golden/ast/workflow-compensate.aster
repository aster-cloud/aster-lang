This module is demo.workflow.compensate.

To processOrder, produce Result of Text and Text. It performs io:
  
  workflow:
    step reserve:
      return ok of "reserved".

    compensate:
      return ok of "released".
    
    step execute:
      return ok of "executed".
  
  .
