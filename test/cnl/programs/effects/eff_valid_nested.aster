This module is test.eff.valid.nested.

To leaf_http, produce Text. It performs io [Http]:
  Return Http.get("/health").

To mid_io, produce Text. It performs io [Http, Sql]:
  Let resp be leaf_http().
  Return Db.insert("logs", resp).

To root_io, produce Text. It performs io [Http, Sql, Time]:
  Let data be mid_io().
  Let ts be Time.now().
  Return +(+(ts, ": "), data).
