This module is demo.eff_infer_transitive.

To leafIO, produce Text. It performs io:
  Return Http.get("https://example.com").

To middleCPU, produce Text. It performs cpu:
  Return leafIO().

To rootIO, produce Text. It performs io:
  Return middleCPU().
