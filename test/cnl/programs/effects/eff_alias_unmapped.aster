This module is test.eff.alias_unmapped.

Use Http as H.

To call_unmapped_alias, produce Text:
  Return X.get("/api").

To call_partial_match, produce Text:
  Return H.get("/api").

To call_nonexistent_prefix, produce Text:
  Return UnknownModule.method().
