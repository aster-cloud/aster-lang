This module is test.capability.v2.

To legacy_io, produce Text. It performs io:
  Return Http.get("https://legacy.example.com/ping").

To fine_grained, produce Text. It performs io [Http]:
  Return Http.get("https://api.example.com/ping").

To mixed, produce Text. It performs io [Files, Secrets]:
  Let token be Secrets.get("capability-demo").
  Let content be Files.read("/tmp/demo.txt").
  Return Text.concat(token, content).
