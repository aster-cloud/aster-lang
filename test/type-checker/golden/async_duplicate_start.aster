This module is tests.typecheck.async.duplicate_start.

To launchTwice with userId: Text, produce Text:
  Start profile as async ProfileSvc.load(userId).
  Start profile as async ProfileSvc.reload(userId).
  Wait for profile.
  Return "done".
