This module is tests.typecheck.workflow.retry_exponential.

To retryExponential, produce Workflow of Result of Text and Text and IO. It performs io:
  
  workflow:
    step only:
      Return ok of "ok".
  
    retry:
      max attempts: 3.
      backoff: exponential.
  
  .
