/**
 * @module config/effects
 *
 * 效果和能力配置（向后兼容导出）。
 *
 * **注意**：此文件现在只是重导出 semantic.ts 的内容，保持向后兼容。
 * 新代码应该直接从 semantic.ts 导入。
 *
 * @deprecated 请直接使用 './semantic.js'
 */

export { IO_PREFIXES, CPU_PREFIXES, CAPABILITY_PREFIXES } from './semantic.js';
